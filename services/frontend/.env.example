# Optional: absolute API base for `vite preview` or custom setups.
# Leave unset for local dev — Vite proxies `/api/v1/*` to todo (8080) and auth (8081).
# In Docker, nginx proxies `/api/v1/*` to service hostnames; the browser stays same-origin.
# VITE_API_BASE=http://localhost:8080
