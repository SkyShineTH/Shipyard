{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "todo-service.serviceAccountName" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "todo-service.labels" . | nindent 4 }}
{{- end }}
